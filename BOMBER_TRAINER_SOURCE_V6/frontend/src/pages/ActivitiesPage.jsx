import React, { useEffect, useMemo, useRef, useState } from 'react';
import Helmet from 'react-helmet';
import { Link } from 'react-router-dom';
import AppShell from '@/components/AppShell';
import pb from '@/lib/pocketBaseClient';
import { TYPES } from '@/lib/btData';
const CLASSIFICATIONS=[['manteniment','Entrenament normal'],['forestal','Prova forestal'],['estructural','Prova estructural'],['aquatic','Prova aquàtica'],['pressbanca','Press banca']];
const labels={running:'Córrer',run:'Córrer',trailrun:'Trail / forestal',trail:'Trail / forestal',swimming:'Natació',swim:'Natació',strength:'Força',weighttraining:'Força',cycling:'Bici',ride:'Bici',virtualride:'Bicicleta estàtica',indoorcycling:'Bicicleta estàtica',spinning:'Bicicleta estàtica'};
const normalizeType=value=>String(value||'').trim().toLowerCase().replace(/[^a-z]/g,'');
const RUNNING_TYPES=new Set(['running','run','trailrun','trail']); const CYCLING_TYPES=new Set(['cycling','ride','virtualride','indoorcycling','spinning']);
function metricNumber(v){const n=Number(v);return Number.isFinite(n)&&n>0?n:null;}
function formatDuration(seconds){const n=metricNumber(seconds);if(!n)return'—';const whole=Math.round(n);const m=Math.floor(whole/60);const remaining=whole%60;return`${m}:${String(remaining).padStart(2,'0')}`;}
function formatPace(seconds,meters){const s=metricNumber(seconds);const m=metricNumber(meters);if(!s||!m)return'—';const whole=Math.round(s/(m/1000));if(!Number.isFinite(whole)||whole<=0||whole>3600)return'—';return`${String(Math.floor(whole/60)).padStart(2,'0')}:${String(whole%60).padStart(2,'0')} /km`;}
function parseWearable(value){if(!value)return{};if(typeof value==='string'){try{return JSON.parse(value)||{};}catch(_){return{};}}return value||{};}
function activityLabel(wearable){const type=normalizeType(wearable?.activityType);return labels[type]||wearable?.activityType||wearable?.name||'Activitat';}
function isRunningActivity(session){const w=parseWearable(session?.wearable);return RUNNING_TYPES.has(normalizeType(w?.activityType));}
function sessionIdentity(session){const w=parseWearable(session?.wearable);const id=String(w?.activityId??'').trim();if(id)return`intervals:${id}`;const start=String(w?.startDateLocal||session?.date||'').slice(0,16);const name=String(w?.name||'').trim().toLowerCase();const type=normalizeType(w?.activityType);if(start||name||type)return`fallback:${start}|${name}|${type}`;return`row:${session?.id}`;}
function dedupeSessions(rows){const groups=new Map();for(const row of rows){const key=sessionIdentity(row);const list=groups.get(key)||[];list.push(row);groups.set(key,list);}const result=[];for(const list of groups.values()){list.sort((a,b)=>{const aClassified=a?.type&&a.type!=='manteniment'?1:0;const bClassified=b?.type&&b.type!=='manteniment'?1:0;if(aClassified!==bClassified)return bClassified-aClassified;return new Date(b?.updated||b?.created||0).getTime()-new Date(a?.updated||a?.created||0).getTime();});result.push(list[0]);}return result;}
function normalizeError(error){const status=Number(error?.status||error?.response?.code||0);const raw=String(error?.response?.message||error?.message||'').trim();if(status===401||status===403)return'Sessió no autoritzada. Torna a iniciar sessió i prova-ho de nou.';if(status===404)return'La col·lecció d’activitats no està disponible al servidor.';if(status===429)return'S’han fet massa peticions. Espera uns segons i torna-ho a provar.';if(status>=500)return'El servidor està temporalment ocupat. Torna-ho a provar en uns segons.';if(/something went wrong|failed to fetch|network|fetch/i.test(raw))return'No s’ha pogut contactar amb el servidor. Comprova la connexió i torna-ho a provar.';return raw||'No s’han pogut carregar les activitats.';}
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
export default function ActivitiesPage(){
 const[sessions,setSessions]=useState([]);const[loading,setLoading]=useState(true);const[loadError,setLoadError]=useState('');const[saving,setSaving]=useState(null);const[filter,setFilter]=useState('all');const loadGeneration=useRef(0);
 async function load(){const generation=++loadGeneration.current;const owner=pb.authStore.record?.id;if(!owner){setLoadError('No hi ha una sessió d’usuari activa.');setLoading(false);return;}setLoading(true);setLoadError('');let lastError=null;for(let attempt=0;attempt<3;attempt+=1){try{const rows=await pb.collection('bt_sessions').getFullList({sort:'-date,-created',filter:`owner = \"${owner}\"`});if(generation!==loadGeneration.current)return;setSessions(dedupeSessions(Array.isArray(rows)?rows:[]));setLoadError('');setLoading(false);return;}catch(error){lastError=error;const status=Number(error?.status||error?.response?.code||0);if((status===401||status===403||status===404||status===400)||attempt===2)break;await sleep(500*(attempt+1));}}if(generation===loadGeneration.current){setLoadError(normalizeError(lastError));setLoading(false);}}
 useEffect(()=>{load();return()=>{loadGeneration.current+=1;};},[]);
 const visible=useMemo(()=>{if(filter==='all')return sessions;if(filter==='pending')return sessions.filter(s=>!s.type||s.type==='manteniment');if(filter==='running')return sessions.filter(isRunningActivity);return sessions.filter(s=>s.type===filter);},[sessions,filter]);
 async function classify(session,type){setSaving(session.id);setLoadError('');try{await pb.collection('bt_sessions').update(session.id,{type,notes:type==='manteniment'?'Activitat sincronitzada · entrenament normal':`Activitat sincronitzada · associada a ${TYPES[type]?.label||type}`});setSessions(prev=>prev.map(s=>s.id===session.id?{...s,type}:s));}catch(error){setLoadError(normalizeError(error));}finally{setSaving(null);}}
 async function deleteOne(session){if(!window.confirm('Vols esborrar aquesta activitat? Aquesta acció no es pot desfer.'))return;setSaving(session.id);setLoadError('');try{await pb.collection('bt_sessions').delete(session.id);setSessions(prev=>prev.filter(s=>s.id!==session.id));}catch(error){setLoadError(normalizeError(error));}finally{setSaving(null);}}
 return <AppShell title="ACTIVITATS"><Helmet><title>Activitats — BOMBER TRAINER</title></Helmet>
  <section className="rounded-3xl bg-white border border-slate-200 p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold tracking-widest text-slate-400">SUUNTO · INTERVALS.ICU</p><h2 className="mt-1 text-xl font-extrabold">Activitats sincronitzades</h2><p className="mt-2 text-sm text-slate-500">Aquí decideixes quines sessions són proves reals de preparació i quines són només entrenament.</p></div><Link to="/configuracio" className="rounded-xl border border-slate-300 px-3 py-2 text-sm font-bold">⚙️ Configuració</Link></div><div className="mt-4 flex flex-wrap gap-2">{[['all','Totes'],['pending','Pendents / normals'],['running','Curses'],['forestal','Forestals'],['estructural','Estructurals'],['aquatic','Aquàtiques'],['pressbanca','Press banca']].map(([key,label])=><button key={key} onClick={()=>setFilter(key)} className={`shrink-0 rounded-full px-4 py-2 text-sm font-bold ${filter===key?'bg-slate-900 text-white':'bg-slate-100 text-slate-600'}`}>{label}</button>)}</div><div className="mt-3 flex items-center justify-between gap-3 text-xs font-semibold text-slate-400"><span>{sessions.length} activitats úniques carregades</span><button type="button" onClick={()=>load()} disabled={loading} className="rounded-lg border border-slate-200 px-3 py-2 text-slate-600 disabled:opacity-50">{loading?'Carregant…':'↻ Recarregar'}</button></div></section>
  {loadError&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-center text-sm font-semibold text-red-700"><p>{loadError}</p><button type="button" onClick={()=>load()} disabled={loading} className="mt-3 rounded-xl bg-red-700 px-4 py-2 text-sm font-bold text-white disabled:opacity-50">Tornar a provar</button></div>}
  {loading?<div className="rounded-2xl bg-white p-6 text-center text-slate-500">Carregant activitats…</div>:visible.length===0?<div className="rounded-2xl bg-white p-6 text-center text-slate-500">{sessions.length===0?'No hi ha activitats sincronitzades. Pots sincronitzar l’historial des de Configuració.':'No hi ha activitats amb aquest filtre.'}</div>:<div className="space-y-3">{visible.map(session=>{const w=parseWearable(session.wearable);const suunto=parseWearable(w.suunto);const heart=w.heartRate&&(w.heartRate.average||w.heartRate.max||w.heartRate.min)?parseWearable(w.heartRate):parseWearable(suunto.heartRate);const dist=metricNumber(w.distanceMeters)||(metricNumber(w.distanceKm)?w.distanceKm*1000:null)||(metricNumber(suunto.distanceKm)?suunto.distanceKm*1000:null);const duration=metricNumber(w.durationSeconds)||metricNumber(suunto.durationSeconds);const calories=metricNumber(w.calories)||metricNumber(suunto.calories);const activityType=normalizeType(w.activityType);const isRunning=RUNNING_TYPES.has(activityType);const isCycling=CYCLING_TYPES.has(activityType);const metrics=isRunning?[['Temps',formatDuration(duration)],['Distància',dist?`${(dist/1000).toFixed(2)} km`:'—'],['Ritme',formatPace(duration,dist)],['FC mitjana',metricNumber(heart.average)?`${Math.round(heart.average)} bpm`:'—'],['FC màxima',metricNumber(heart.max)?`${Math.round(heart.max)} bpm`:'—']]:isCycling?[['Temps',formatDuration(duration)],['Calories',calories?`${Math.round(calories)} kcal`:'—'],...(dist?[['Distància',`${(dist/1000).toFixed(2)} km`]]:[]),['FC mitjana',metricNumber(heart.average)?`${Math.round(heart.average)} bpm`:'—'],['FC màxima',metricNumber(heart.max)?`${Math.round(heart.max)} bpm`:'—']]:[['Temps',formatDuration(duration)],...(dist?[['Distància',`${(dist/1000).toFixed(2)} km`]]:[]),...(calories?[['Calories',`${Math.round(calories)} kcal`]]:[]),['FC mitjana',metricNumber(heart.average)?`${Math.round(heart.average)} bpm`:'—'],['FC màxima',metricNumber(heart.max)?`${Math.round(heart.max)} bpm`:'—']];return <article key={session.id} className="rounded-3xl bg-white border border-slate-200 p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold text-slate-400">{session.date||w.startDateLocal?.slice?.(0,10)||'Sense data'}</p><h3 className="mt-1 font-extrabold">{activityLabel(w)}</h3>{w.name&&<p className="text-xs text-slate-500">{w.name}</p>}</div><div className="flex items-start gap-2"><span className="rounded-full px-3 py-1 text-xs font-bold" style={{backgroundColor:TYPES[session.type]?.soft||'#f1f5f9',color:TYPES[session.type]?.color||'#475569'}}>{session.type==='manteniment'?'Entrenament normal / pendent':TYPES[session.type]?.label||'Pendent'}</span><button type="button" aria-label="Esborrar activitat" title="Esborrar activitat" onClick={()=>deleteOne(session)} disabled={saving===session.id} className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full border border-red-200 bg-red-50 text-lg font-black text-red-600 hover:bg-red-100 disabled:opacity-50">×</button></div></div><div className={`mt-3 grid grid-cols-2 gap-2 ${metrics.length>=5?'sm:grid-cols-5':'sm:grid-cols-4'}`}>{metrics.map(([label,value])=><div key={label} className="rounded-xl bg-slate-50 p-3"><span className="text-xs text-slate-500">{label}</span><strong className="block">{value}</strong></div>)}</div>{metricNumber(w.trainingLoad)&&<p className="mt-2 text-xs text-slate-500">Càrrega Intervals.icu: <b>{Math.round(w.trainingLoad)}</b></p>}<div className="mt-4"><p className="mb-2 text-xs font-bold uppercase tracking-widest text-slate-400">Associar aquesta sessió a</p><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{CLASSIFICATIONS.map(([type,label])=><button key={type} disabled={saving===session.id} onClick={()=>classify(session,type)} className={`min-h-[44px] rounded-xl border px-3 text-sm font-bold ${session.type===type?'border-slate-900 bg-slate-900 text-white':'border-slate-200 bg-white text-slate-700'}`}>{label}</button>)}</div></div></article>;})}</div>}
 </AppShell>;
}
